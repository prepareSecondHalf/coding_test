정렬
질문1
교재 p.160에서 선택 정렬의 연산 횟수는 N + (N - 1) + (N - 2) + (N - 3) + ... + 2 라고 나와 있다.
연산 횟수가 점점 줄어듬에 따라 N에서 M이 0이고 탐색 할 때마다 1씩 증가할 때,  N - M씩 된다는 것을 알겠다.
그런데 마지막에 정수 2가 나오는 이유가 무엇인지 이해가 되질 않는다.

질문2 / 이취코, 위에서 아래로
# array = array.sort() -> 정렬 후 array를 출력하면, None 값이 출력된다. why?
